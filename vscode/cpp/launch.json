{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CMake-built target",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetProgramPath}",
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}"
        }
    ]
}
